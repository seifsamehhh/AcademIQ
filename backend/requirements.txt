# AcademIQ backend — auth + student dashboard (Vercel serverless / local uvicorn)
# Local full ML stack: pip install -r requirements-full.txt

fastapi
uvicorn[standard]
pymongo>=3.12,<5
certifi
pydantic[email]>=2
python-jose[cryptography]
bcrypt>=4.0.1,<5
passlib[bcrypt]>=1.7.4
python-dotenv
numpy
