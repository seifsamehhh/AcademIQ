# AcademIQ ML service — Performance Model v4 only (Hugging Face Docker Space)
# Independent from backend/requirements.txt — do not merge with Vercel slim deps.

fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
pydantic>=2,<3

numpy>=1.26,<3
pandas>=2.1,<3
joblib>=1.3,<2
# Artifacts were trained with sklearn 1.6.x — pin for HF Docker loads
scikit-learn>=1.6.1,<1.7
lightgbm>=4.3,<5
shap>=0.45,<1
