# Copy to `.env.local` for local development.
# On Vercel (frontend project), set in Project → Settings → Environment Variables.

# Required — your backend Vercel URL (no trailing slash).
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Must be false in production so the app calls the real API.
NEXT_PUBLIC_USE_MOCK=false
